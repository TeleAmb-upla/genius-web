# Earth Engine OAuth (never commit)
secrets/earthengine_credentials.json
secrets/*.json
!secrets/README.md

# Large drone RGB source (~6 GB); keep out of git — viewer uses WebP + bounds JSON
assets/data/raster/Iluminacion/M3T_RGB_QUI_2024_07*.tif

# OS-generated noise
**/desktop.ini

# Python cache / bytecode
**/__pycache__/
*.py[cod]

# Local pipeline state
scripts/gee/drive/drive_sync_keys_state.json
scripts/gee/drive_sync_keys_state.json

# Accidental duplicate downloads
*.geojson(1)
