# Google Earth Engine (Python API) — exportadores NDVI para Plataforma GENIUS.
# Antes de usar `ee` en un script nuevo del paquete: from .earth_engine_init.ee_init import initialize_ee; initialize_ee()
